# Open Brain — local environment variables.
# Copy this file to `.env` (gitignored) and fill in the real values.

# GitHub OAuth App client_id with Device Flow enabled.
# Public and safe to bundle — the Device Flow is designed to work without a secret.
# See README → "GitHub OAuth App setup" for how to obtain this.
VITE_GITHUB_CLIENT_ID=
